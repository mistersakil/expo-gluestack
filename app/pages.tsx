import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Pages = () => {
  return (
    <View>
      <Text>Router Pages</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Pages;
